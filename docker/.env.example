# 复制到同目录的 .env（即 docker/.env）后填写。.env 已被 .gitignore 忽略，不会入库。
# docker compose 会自动从「compose 文件所在目录」读取 .env，所以本文件必须放在 docker/ 下。

# 单用户登录口令（强烈建议设置）：家卫 Web UI 的登录口令。
# 不填则服务启动时会自动生成随机口令，仅在日志里打印一次（可信局域网可用，
# 若暴露到公网务必显式设一个强口令）。
HOMEWARD_AUTH_TOKEN=

# 可选：dnsmasq 查询日志路径。留空走自动探测（/var/log/dnsmasq.log 等）。
# 飞牛 / 其它发行版若 DNS 日志不在默认路径，在这里写绝对路径，会传给 --dns-log。
# DNS_LOG_PATH=/var/log/dnsmasq.log
