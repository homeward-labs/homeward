# 运行时数据 / 日志（含用户隐私，绝不入库）
data/
logs/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store

# 本地配置（可能含密钥）
*.local.yaml
config.local.*
.env

# 产物
*.png.bak
screenshots_local/

# ===== 闭源版本（标准版 / 专业版）—— 绝不上传公开仓库 =====
# 以下目录只存在于本地/私有仓库，公开仓库（社区版）永不收录。
src/enforcers
src/editions/standard
src/editions/pro
# 任何文件若带闭源标记（见 docs/EDITIONS.md），即使不在上面目录也会被 pre-commit 拦截

